{
  "name": "oauth-sample",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\"  \"npm run dev -w client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0",
    "tsc": "^2.0.4",
    "typescript": "^5.6.3"
  }
}
